<div class="w-full max-w-xs">
  <div class="mb-4">
    <div class="text-lg"> </div>
  </div>

  <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
    <div class="flex flex-col space-y-4">
      <% @available_jobs.each do |job| %>
        <div><%= "Enqueue #{job.name}" %></div>
      <% end %>
    </div>
  </div>
</div>
